while (true) {
    //Math.random() => int i;    
    Math.random2(15, 20) => int i;    
    <<< i >>>; 
    
    //Math.randomf() => float f;
    Math.random2f(-1.67, 3.45) => float f;
    <<< f >>>;
     
    .5::second => now;
}